UWSGI_WORKERS=2 # количество процессов UWSGI
UWSGI_THREADS=2 # количество потоков UWSGI
JWT_SECRET='very_secret_jwt' # секретное значение для JWT токенов
EXTERNAL_HOST='https://example.com' # Хост на котором будет крутиться приложение, будет добавляться к ссылке на файл